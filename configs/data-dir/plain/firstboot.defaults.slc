{{define "plain/firstboot.defaults" -}}
# Rendered by Shoelaces (Go template). Values come from URL query params.
# Example fetch:
#   /configs/plain/firstboot.defaults?firstboot_message=hello

FIRSTBOOT_MESSAGE={{ if .firstboot_message }}{{ .firstboot_message }}{{ else }}hello from shoelaces{{ end }}
{{end}}
